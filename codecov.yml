# SPDX-FileCopyrightText: 2017 Alex Brandt
# SPDX-License-Identifier: MIT

# hpc-codecov reports every module in the .tix, the test suite included.
# "test/**/*" would compile to a regex requiring a further slash, missing
# test/Main.hs.
ignore:
  - "test/**"

coverage:
  status:
    project:
      default:
        threshold: 1%
    patch:
      default:
        threshold: 1%
